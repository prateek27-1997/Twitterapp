<h1> New Tweet</h1>
<%= form_for @tweet do |f| %>
  <p>
  	<%= f.label :tweet %> <br>
  	<%= f.text_area :content %>
  </p>
      <p>
      	<%= f.submit %>
      </p>
  <% end %>
  <%= link_to 'Back', tweets_path %>

